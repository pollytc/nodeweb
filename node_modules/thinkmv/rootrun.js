/**
 * Created by Administrator on 2016/12/22.
 */
var local = require('./local')
module.exports=function (think,req,res,next) {
    var user = think.global.getMarker();
    if(user){
        local.login = 1;
        local.logname = user.username;
    }else{
        local.login = 0;
    }
}